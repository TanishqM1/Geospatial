apiVersion: v1
kind: Service
metadata:
  name: geospatial
  labels:
    app: geospatial
spec:
  type: NodePort
  selector:
    app: geospatial
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
